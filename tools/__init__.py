"""Standalone tool pipelines (CLAUDE.md §8).

Empty until Phase 2: summarizer.py, downloader.py, ocr.py, rag.py land here,
each importable as a plain module so the harness can also expose them as
function-calling tools later.
"""
