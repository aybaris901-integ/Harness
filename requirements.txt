# Phase 1 — bot skeleton + tutor.
# Phase 2+ deps (trafilatura, yt-dlp, faster-whisper, chromadb, ...) get added
# in their own phases.

aiogram>=3.15,<4
aiosqlite>=0.20
httpx>=0.27
python-dotenv>=1.0
